1. Install dotnet core;
2. Install node.js (with npm);
3. Install and run mongoDB;
4. Execute build.bat;
5. Fill required parameters;
6. Wait for a few minutes until npm downloads all required packages, build UI package, dotnet core projects got restored and all required services got started;
7. Visit http://localhost:8000.